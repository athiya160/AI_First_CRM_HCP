import React from 'react';

const Settings = () => {
  return (
    <div style={{ padding: '24px' }}>
      <h1>Settings</h1>
      <p style={{ color: 'var(--text-muted)' }}>Preferences and configurations will live here.</p>
    </div>
  );
};

export default Settings;
